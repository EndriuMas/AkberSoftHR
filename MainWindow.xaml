<Window x:Class="AkberSoft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="АКБЕРСОФТ" Height="550" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- Зеленая шапка сверху -->
        <Border Height="68" Background="#2E7D32" VerticalAlignment="Top" Margin="10,0,10,0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="АКБЕРСОФТ" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Система управления персоналом" FontSize="16" Foreground="LightGray" Width="8" RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="1"/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Основной контент (с отступом от зеленой шапки) -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="🔄 Обновить" Width="110" Height="35" Margin="5" Click="RefreshBtn_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
                <Button Content="➕ Добавить" Width="110" Height="35" Margin="5" Click="AddBtn_Click" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
                <Button Content="✏ Изменить" Width="110" Height="35" Margin="5" Click="EditBtn_Click" Background="#FF9800" Foreground="White" FontWeight="Bold"/>
                <Button Content="🗑 Удалить" Width="110" Height="35" Click="DeleteBtn_Click" Background="#F44336" Foreground="White" FontWeight="Bold"/>
            </StackPanel>

            <!-- Таблица -->
            <DataGrid x:Name="MyDataGrid" Grid.Row="1" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      AlternatingRowBackground="#F5F5F5"
                      FontSize="13"
                      RowHeight="35">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="180"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="150"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="Команда / Отдел" Binding="{Binding Department}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
